import 'package:flutter/material.dart';

class TruckDisptachScreen extends StatelessWidget {
  const TruckDisptachScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Truck Dispatch Dashboard"),
      ),
    );
  }
}
